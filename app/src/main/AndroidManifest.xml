<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icon_enak"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RestaurantApp"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StoreDetailActivity">
        </activity>
        <activity android:name=".MenuActivity">
            <!-- Add any intent filters or other activity properties here if needed -->
        </activity>
        <activity android:name=".BurgerActivity"></activity>
        <activity android:name=".PizzaActivity"></activity>
        <activity android:name=".SpaghettiActivity"></activity>
        <activity android:name=".SteakActivity"></activity>
        <activity android:name=".OrderActivity"></activity>
    </application>


</manifest>